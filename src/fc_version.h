#pragma once
// fc_version.h -- the firmware version, in one place (E4, #274).
// Bump it here before tagging; it must match the tag (CLAUDE.md, Versioning).
// Shown on the TFT boot banner, the About screen and every web page.
#define FW_VERSION "0.51.1"
