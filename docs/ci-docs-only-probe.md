# Docs-only probe (C3, #223)

Throwaway. This file exists only so a pull request can touch a documentation
path and nothing else, proving that `compile-gate` reports green while the
`build` job is skipped entirely.

Deleted once the probe is recorded.
